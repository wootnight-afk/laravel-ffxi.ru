# Local MySQL 8.4 configuration for the dev Docker stack.
# Mounted at /etc/mysql/conf.d/my.cnf inside the container.
# Production MySQL is provided by Timeweb (spec section 7).
[mysqld]
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci
[client]
default-character-set=utf8mb4
