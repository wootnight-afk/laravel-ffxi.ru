PROJECT CONTEXT — MANDATORY

The full technical specification for this project is located at:
    docs/ai/context.md

You MUST read this file at the start of EVERY session and BEFORE ANY task,
file creation, or code generation.

This file contains:
- mandatory architecture constraints,
- locked stack decisions,
- deployment and production rules,
- security requirements,
- forbidden actions.

These requirements OVERRIDE any of your default assumptions.

If you have not read docs/ai/context.md in the current session,
you are NOT allowed to proceed with any file creation, modification,
or code generation.

WORK FORMAT (section 34 of the spec):
- Step 1 — provide architecture summary and wait for confirmation.
- Step 2 — after confirmation, generate files one at a time, complete,
  no placeholders.
- Step 3 — if any contradiction, incompatibility, or missing capability
  is found, STOP and report before continuing.

LANGUAGE:
- Communication with the user: Russian.
- Code, comments, commit messages: English.

============================================================
CRITICAL CONSTRAINTS — DO NOT VIOLATE
============================================================

## Working directory

The ONLY allowed project directory is:

    /home/skyw/projects/laravel-ffxi.ru

You MUST NOT:
- execute commands outside this directory,
- explore other projects in /home/skyw/projects/* or elsewhere,
- attempt to cd to /home/alex/... or any path with a different user,
- read files outside this project,
- look at unrelated projects (admin-api, hotel-booking, cloud-storage,
  bitrix-project) — they exist but are NOT related to this task.

If you need information from outside — ASK the user and WAIT.

## No unrequested exploration

You MUST NOT ask about or research topics not explicitly present in
docs/ai/context.md. In particular:
- Do NOT ask about PHP 8.5 EOL — the spec requires PHP 8.4.
- Do NOT look at third-party blogs (travisbean.com, medium.com, dev.to, etc.).
- Only use: packagist.org, repo.packagist.org, official docs
  (laravel.com/docs, filamentphp.com/docs, livewire.laravel.com),
  github.com raw files of official repositories.

============================================================
CURRENT STATE — CONFIRMED DATA (already collected)
============================================================

Verified via Packagist (September 2026):

- laravel/framework          13.33.0    requires PHP ^8.3
- filament/filament          5.8.4      requires PHP ^8.2
- livewire/livewire          4.4.6      requires PHP ^8.1,
                                          supports Laravel ^10|^11|^12|^13
- spatie/laravel-permission  8.3.0      requires PHP ^8.3,
                                          Laravel ^12|^13
- pestphp/pest               5.2.1      requires PHP ^8.4
- larastan/larastan          3.12.2     requires PHP ^8.2,
                                          Laravel ^11|^12|^13
- laravel/pint               1.32.1     requires PHP ^8.3
- intervention/image         4.3.2      requires PHP ^8.3

NOT in composer.json (deliberately excluded):
- laravel/fortify — remains a candidate for stage 4 (MFA), NOT part
  of the stack. Add only via separate PR with ADR justification if
  Filament 5 does not provide MFA out of the box.

Still MISSING:
- Filament 4 data (last stable 4.x, PHP and Laravel constraints).
  Needed for ADR-001 comparison per section 3 of the spec.

============================================================
CURRENT TASK — WHAT TO DO NOW
============================================================

Context: Step 1 (architecture summary) was already delivered and
accepted with corrections. We are now on the correction round before
Step 2 (file generation).

Do the following, in order:

1. Collect Filament 4 data via Packagist API — last stable 4.x version,
   PHP constraint, Laravel constraint. If the request fails after up
   to 3 attempts with 5s pause — say honestly:
   "Filament 4 data unavailable, manual verification required."
   Do NOT consult third-party blogs.

2. Produce UPDATED point 2 (Chosen versions) and point 4 (Compatibility
   check) of Step 1 only. Do NOT redo other points.

3. Point 2 (Chosen versions) MUST NOT include laravel/fortify.

4. Point 4 (Compatibility check) MUST be a table built only from
   concrete constraints (package → requirement → environment → verdict).
   NO "works with" phrasing.

5. In ADR-001 the CVE wording MUST be literal:
   "Security advisories are checked automatically via composer audit
   in CI (section 21 of the spec) and monthly via composer outdated
   --direct (section 20 of the spec). No claims about the absence of
   CVE are made in ADR-001."

6. Livewire as explicit require — keep. One-line justification for
   ADR-001: "Explicit require livewire/livewire: control over the
   critical transitive dependency of Filament 5; version pinned via
   composer.lock."

7. ADR-001 must record ONE of:
   — "Both stacks (Filament 5 and Filament 4) are workable; chosen
     by tie-break rule (section 3): longer support window, wider
     plugin coverage, shorter upgrade path";
   — "Filament 4 rejected: incompatible with Laravel 13 (specify
     concrete constraint)".

Do NOT create any files.
Do NOT proceed to Step 2.
Stop and wait for confirmation.

